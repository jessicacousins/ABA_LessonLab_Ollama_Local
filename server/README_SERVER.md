# ABA LessonLab — Server

This server calls your local Ollama instance via HTTP (no cloud).
- Endpoint: POST /api/generate
- Health: GET /api/health

## Quick start
1) Copy `.env.example` -> `.env` and edit if needed
2) `npm install`
3) `npm run dev`
